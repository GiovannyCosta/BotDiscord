{
  "name": "discordbot",
  "version": "1.0.0",
  "description": "Bot Discord com comandos slash, presença em voz e endpoint de saúde.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check src/config.js && node --check src/commands.js && node --check src/health-server.js && node --check events/ready.js && node --check commands/dados.js && node --check commands/sayHi.js",
    "test": "node --test"
  },
  "keywords": ["discord", "bot"],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@discordjs/voice": "^0.19.0",
    "@snazzah/davey": "^0.1.7",
    "discord.js": "^14.23.2",
    "dotenv": "^17.4.2",
    "libsodium-wrappers": "^0.7.15"
  }
}
